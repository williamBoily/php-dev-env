version: "3.8"
services:
  php:
    container_name: php
    build: './docker/php/'
    networks:
      - backend
    volumes:
      - ./php_sandbox/:/var/www/project/
  apache:
    container_name: web
    build: './docker/apache/'
    depends_on:
      - php
      #- mysql
    networks:       
      #- frontend
      - backend
    ports:
      - "8080:80"
    volumes:
      - ./php_sandbox/:/var/www/project/
#  mysql:
#    image: mysql:5.6.40
#    networks:
#      - backend
#    environment:
#      - MYSQL_ROOT_PASSWORD=rootpassword
networks:
#  frontend:
  backend: