version: "3.8"
services:
  php:
    container_name: php
    build: './docker/php/'
    networks:
      - backend
    volumes:
      - ./app/src/:/var/www/html/
  apache:
    container_name: web
    build: './docker/apache/'
    depends_on:
      - php
      #- mysql
    networks:       
      #- frontend
      - backend
    ports:
      - "8080:80"
    volumes:
      - ./app/src/:/var/www/html/
#  mysql:
#    image: mysql:5.6.40
#    networks:
#      - backend
#    environment:
#      - MYSQL_ROOT_PASSWORD=rootpassword
networks:
#  frontend:
  backend: